var states = [
    "Bihar",
    "UP","MP","Maharastra",300,"Tamil Nadu","AP"];
    states.forEach((s)=>(console.log(s)));
    //NOte- whenever there is an array use for each method as the memory space and index are already alloted so no need to 
    //initialize as this method goes through every index and stops at the end.